<%= form_for(@user_feedback_note) do |f| %>
  <% if @user_feedback_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_feedback_note.errors.count, "error") %> prohibited this user_feedback_note from being saved:</h2>

      <ul>
      <% @user_feedback_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :feedback_id %><br>
    <%= f.number_field :feedback_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
