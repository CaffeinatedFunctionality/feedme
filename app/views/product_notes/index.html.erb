<h1>Listing product_notes</h1>

<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>Isprivate</th>
      <th>User</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @product_notes.each do |product_note| %>
      <tr>
        <td><%= product_note.product_id %></td>
        <td><%= product_note.isprivate %></td>
        <td><%= product_note.user_id %></td>
        <td><%= product_note.content %></td>
        <td><%= link_to 'Show', product_note %></td>
        <td><%= link_to 'Edit', edit_product_note_path(product_note) %></td>
        <td><%= link_to 'Destroy', product_note, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Product note', new_product_note_path %>
